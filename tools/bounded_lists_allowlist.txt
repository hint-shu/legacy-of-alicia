# Гейт ограниченных списков (R74, backlog #170) — список исключений.
#
# ФОРМАТ: <путь>|<полное имя функции>|<контейнерное выражение>|<причина>
#
# ★КЛЮЧ БЕЗ НОМЕРА СТРОКИ, И ЭТО НЕ УДОБСТВО, А ТРЕБОВАНИЕ. Свип раунда правит
# 23 площадки в одном только RanchMessageDefinitions.cpp, из них 20 выше строки
# 3161 — ключ по номеру строки протух бы НА САМОМ КАНДИДАТЕ, и критерий приёмки
# «гейт зелёный на кандидате» стал бы невыполнимым. Номер строки допускается
# только в хвостовом комментарии и гейтом не читается.
#
# ★ГЕЙТ ПАДАЕТ И В ОБРАТНУЮ СТОРОНУ: запись, которая больше ни на что не
# указывает (файл+функция+выражение не нашлись), — ОШИБКА, чтобы список не гнил.
src/libserver/network/command/proto/RanchMessageDefinitions.cpp|AcCmdCRRequestDailyQuestRewardOK::Reward::Write|value.items|список без счётчика на проводе вообще; вызывающих нет (проверено grep'ом по дереву); поведение раунд не меняет  # ~3161
