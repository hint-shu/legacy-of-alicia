// Фикстура гарда tools/check_field_init.py. НЕ КОД СЕРВЕРА.
// R72-fix3-2 (находка Codex 2, итерация 3). Ожидание: КОД 2 (проверка
// недействительна). Смешанное объявление за телом встроенного перечисления:
// ПЕРВЫЙ декларатор инициализирован, ВТОРОЙ — нет. Ветка встроенного типа
// выходила раньше остановки «несколько деклараторов», видела `{` в тексте и
// засчитывала ВСЁ объявление как одно инициализированное поле: ноль
// нарушителей, ноль неразобранных, а `other` — неинициализированный скаляр
// того самого класса #174 — исчезал молча. Инициализатор у первого
// декларатора здесь не украшение: именно он делал остановку ненужной на вид.
#ifndef FIXTURE_INLINE_ENUM_MIXED_STOP_HPP
#define FIXTURE_INLINE_ENUM_MIXED_STOP_HPP

#include <cstdint>

namespace fixture
{

struct InlineEnumMixedStop
{
  enum class Part
  {
    Body = 0,
    Mane = 1
  } part{Part::Body}, other;
};

} // namespace fixture

#endif
