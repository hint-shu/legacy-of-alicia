# Generated by quests/gen_achievements_yaml.py — DO NOT EDIT BY HAND.
# Achievement definitions derived from the game client's libconfig tables
# (Achievements, AchvEventPropertyLink); payout values are our own
# (cosmetic-only).
#
# ВСЕГО: 246 достижений.
#   source: server — 202 (82%): событие приходит
#     серверу его собственными командами, репорт клиента не нужен ВООБЩЕ.
#   source: client — 44: событие приходит только командой 0x16b,
#     то есть со слов клиента. Прогресс по ним допустим, выплата — нет.
#   Условие реализовано в оригинале: 195.
#   Платили морковками в оригинале: 151 (у нас выплаты обнулены).
#
# ★Почему это деление важно: 202 достижений не зависят от клиента, а значит
# не заложники ни его честности, ни дефекта #77 — их можно включать первыми.
achievements:
  collection:
    # Все рывки в дело
    - tid: 10001
      name: "Все рывки в дело"
      book: -2
      event: 17
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "ConsumeSpur"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 3
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [100, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Первый удачный прыжок
    - tid: 10002
      name: "Первый удачный прыжок"
      book: -2
      event: 18
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "OneJump"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 3
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [100, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Первая победа - и сразу до слёз
    - tid: 10003
      name: "Первая победа - и сразу до слёз"
      book: -2
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "MyFirstWin"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 3
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [100, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Горечь поражений
    - tid: 10004
      name: "Горечь поражений"
      book: -2
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "PainOfLosses"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 15
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [100, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Деревня Ривиан 2:30
    - tid: 10005
      name: "Деревня Ривиан 2:30"
      book: -2
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "RiLand03Mastery"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 15
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [100, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Первый рывок с заноса
    - tid: 10006
      name: "Первый рывок с заноса"
      book: -2
      event: 19
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "LearnDash"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 3
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [100, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Ввысь, к небесам
    - tid: 10007
      name: "Ввысь, к небесам"
      book: -2
      event: 25
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: client
      property: "GlidingCount"
      function: "FloatInTheAir"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 3
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [100, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Чистая победа
    - tid: 10008
      name: "Чистая победа"
      book: -2
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "PerfectWin"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 15
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [100, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Каньон Гранто 2:40
    - tid: 10009
      name: "Каньон Гранто 2:40"
      book: -2
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "RiFore02Mastery"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 15
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [100, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Лес Уллен 2:20
    - tid: 10010
      name: "Лес Уллен 2:20"
      book: -2
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "RiFore01Mastery"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 15
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [100, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Поля Побеа 01
    - tid: 10011
      name: "Поля Побеа 01"
      book: -2
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "land02Mastery"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: false
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 15
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [100, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Бывалый игрок
    - tid: 10012
      name: "Бывалый игрок"
      book: -2
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "Playing20Races"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 15
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [100, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Мастерство доказано
    - tid: 10013
      name: "Мастерство доказано"
      book: -2
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "SerialWin"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 15
      numPlayer: 1
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [100, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Красный Дорфский торговый дом 2:30
    - tid: 10014
      name: "Красный Дорфский торговый дом 2:30"
      book: -2
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "RiDorf04Mastery"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 15
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [100, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Ввысь, к небесам
    - tid: 10015
      name: "Ввысь, к небесам"
      book: -2
      event: 0
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "Gliding50M"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 15
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [100, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Поля Побеа 2:20
    - tid: 10016
      name: "Поля Побеа 2:20"
      book: -2
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "RiLand02Mastery"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 15
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [100, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Холм Сильфа 2:10
    - tid: 10017
      name: "Холм Сильфа 2:10"
      book: -2
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "RiLand04Mastery"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 15
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [100, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Ранчо Стато 2:20
    - tid: 10018
      name: "Ранчо Стато 2:20"
      book: -2
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "RiLand01Mastery"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 15
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [100, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Занос? А что это?
    - tid: 10019
      name: "Занос? А что это?"
      book: -2
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "NoSlidingWin"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 3
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [100, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Игра в заносах
    - tid: 10020
      name: "Игра в заносах"
      book: -2
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "Sliding20"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 3
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [100, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Рывок! Рывок! Рывок!
    - tid: 10021
      name: "Рывок! Рывок! Рывок!"
      book: -2
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "UseSpur40"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 3
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [100, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Главное для лошади - бег!
    - tid: 10022
      name: "Главное для лошади - бег!"
      book: -2
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "NoJumpWin"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 3
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [100, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Сказочная команда
    - tid: 10023
      name: "Сказочная команда"
      book: -2
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "TeamPerfectWin"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 3
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [100, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Мастер одинарного прыжка
    - tid: 10024
      name: "Мастер одинарного прыжка"
      book: -2
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "Mission_Movejump01_01"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 16
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [100, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Мастер двойного прыжка
    - tid: 10025
      name: "Мастер двойного прыжка"
      book: -2
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "Mission_Movejump01_02"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 16
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [100, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Мастер планирования
    - tid: 10026
      name: "Мастер планирования"
      book: -2
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "Mission_Moveglide01_01"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 16
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [100, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Маг ордена
    - tid: 10027
      name: "Маг ордена"
      book: -2
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "Magic_TripleWin"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 4
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [100, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Садовник адского цветка
    - tid: 10028
      name: "Садовник адского цветка"
      book: -2
      event: 21
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "Magic_NiceFlower"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 12
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [100, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Ледяной шторм!
    - tid: 10029
      name: "Ледяной шторм!"
      book: -2
      event: 22
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "Magic_NiceRange"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 12
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [100, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Сбит!
    - tid: 10030
      name: "Сбит!"
      book: -2
      event: 22
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "Magic_CutHotRodding"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 12
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [100, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Весёлый рывок
    - tid: 10032
      name: "Весёлый рывок"
      book: -2
      event: 17
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "OneSpur"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 3
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [1, 2, 3, 4]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 0, 0, 0]
      points: 10
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Занос с замиранием сердца
    - tid: 10033
      name: "Занос с замиранием сердца"
      book: -2
      event: 26
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: client
      property: "SlidingCount"
      function: "OneSliding"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 31
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [1, 2, 3, 4]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 0, 0, 0]
      points: 10
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Свежий ветер планирования
    - tid: 10034
      name: "Свежий ветер планирования"
      book: -2
      event: 25
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: client
      property: "GlidingCount"
      function: "OneGliding"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 31
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [1, 2, 3, 4]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 0, 0, 0]
      points: 10
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Первый финиш на 1 месте
    - tid: 10035
      name: "Первый финиш на 1 месте"
      book: -2
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "GoalInRanking"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 31
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Обидный сход
    - tid: 10036
      name: "Обидный сход"
      book: -2
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "Retire"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 31
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [1, 2, 3, 4]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 0, 0, 0]
      points: 10
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Волнующая покупка
    - tid: 10037
      name: "Волнующая покупка"
      book: -2
      event: 3
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "BuyItemCount"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [1, 2, 3, 4]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 0, 0, 0]
      points: 10
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Мой первый рост
    - tid: 10038
      name: "Мой первый рост"
      book: -2
      event: 5
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "Level"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [1, 2, 3, 4]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 0, 0, 0]
      points: 10
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Первый рост лошади
    - tid: 10039
      name: "Первый рост лошади"
      book: -2
      event: 6
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "HorseLevel"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [1, 2, 3, 4]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 0, 0, 0]
      points: 10
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Лошадь из разведения
    - tid: 10040
      name: "Лошадь из разведения"
      book: -2
      event: 19
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "BreedSuccessCount"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: false
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Первый праздник
    - tid: 10041
      name: "Первый праздник"
      book: -2
      event: 19
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "FestivalJoinCount"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: false
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Первая записка
    - tid: 10042
      name: "Первая записка"
      book: -2
      event: 19
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "MessageSend"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: false
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Первый друг
    - tid: 10043
      name: "Первый друг"
      book: -2
      event: 19
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "FriendCount"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: false
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Заезд с другом
    - tid: 10044
      name: "Заезд с другом"
      book: -2
      event: 19
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "InGameFriendsCount"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: false
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 31
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Неприятный опыт
    - tid: 10045
      name: "Неприятный опыт"
      book: -2
      event: 16
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "Kicked"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [1, 2, 3, 4]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 0, 0, 0]
      points: 10
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Вступить в орден
    - tid: 10046
      name: "Вступить в орден"
      book: -2
      event: 19
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "GuildJoin"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: false
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Сделать подарок
    - tid: 10047
      name: "Сделать подарок"
      book: -2
      event: 19
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "GiftSendCount"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: false
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Первый сброс
    - tid: 10048
      name: "Первый сброс"
      book: -2
      event: 19
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "ResetCount"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: false
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 31
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Прямо как супергерой
    - tid: 10049
      name: "Прямо как супергерой"
      book: -2
      event: 27
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: client
      property: "GlidingSpurCount"
      function: "OneGlidingSpur"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 31
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [1, 2, 3, 4]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 0, 0, 0]
      points: 10
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Первый скриншот
    - tid: 10050
      name: "Первый скриншот"
      book: -2
      event: 19
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "OneScreenShot"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: false
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 31
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Хорошее начало
    - tid: 10051
      name: "Хорошее начало"
      book: -2
      event: 28
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: client
      property: "PerfectStart"
      function: "PerfectStart"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 31
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [1, 2, 3, 4]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 0, 0, 0]
      points: 10
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Первая полная шкала рывка с заноса
    - tid: 10052
      name: "Первая полная шкала рывка с заноса"
      book: -2
      event: 19
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "DashFullGauge"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: false
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 31
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Догоняйте, если сможете
    - tid: 10053
      name: "Догоняйте, если сможете"
      book: -2
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "OtherRetirePlayerCount"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: false
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 31
      numPlayer: 1
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Больше не новичок
    - tid: 10054
      name: "Больше не новичок"
      book: -2
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "Win"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 5
      numPlayer: 8
      compareType: 0
      successType: 1
      thresholds: [1, 2, 3, 4]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 0, 0, 0]
      points: 10
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Во всей красе
    - tid: 10055
      name: "Во всей красе"
      book: -2
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "Win"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 5
      numPlayer: 8
      compareType: 0
      successType: 1
      thresholds: [3, 4, 5, 6]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 0, 0, 0]
      points: 10
      resetEvent: 2
      resetFunction: "Lose"
      resetValue: 0
    # Не остановить
    - tid: 10056
      name: "Не остановить"
      book: -2
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "Win"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 31
      numPlayer: 1
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: "NotWin"
      resetValue: 0
    # Во всей красе
    - tid: 10057
      name: "Во всей красе"
      book: -2
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "Win"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 5
      numPlayer: 8
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [100, 0, 0, 0]
      points: 1
      resetEvent: 2
      resetFunction: "NotWin"
      resetValue: 0
    # Нет равных
    - tid: 10058
      name: "Нет равных"
      book: -2
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "Win"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 31
      numPlayer: 1
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: "NotWin"
      resetValue: 0
    # Я и есть NO.1
    - tid: 10059
      name: "Я и есть NO.1"
      book: -2
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "Win"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 31
      numPlayer: 1
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: "NotWin"
      resetValue: 0
    # Повезло с командой!?
    - tid: 10060
      name: "Повезло с командой!?"
      book: -2
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "TeamWin"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 10
      numPlayer: 8
      compareType: 0
      successType: 1
      thresholds: [1, 2, 3, 4]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 0, 0, 0]
      points: 10
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Мы - одно целое
    - tid: 10061
      name: "Мы - одно целое"
      book: -2
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "TeamWin"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 10
      numPlayer: 8
      compareType: 0
      successType: 1
      thresholds: [3, 4, 5, 6]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 0, 0, 0]
      points: 10
      resetEvent: 2
      resetFunction: "TeamLose"
      resetValue: 0
    # Мы - одно целое
    - tid: 10062
      name: "Мы - одно целое"
      book: -2
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "Win"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 10
      numPlayer: 8
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [100, 0, 0, 0]
      points: 1
      resetEvent: 2
      resetFunction: "NotTeamWin"
      resetValue: 0
    # Сказочная сыгранность
    - tid: 10063
      name: "Сказочная сыгранность"
      book: -2
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "TeamWin"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 10
      numPlayer: 1
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: "NotTeamWin"
      resetValue: 0
    # Пусть приходит кто хочет
    - tid: 10064
      name: "Пусть приходит кто хочет"
      book: -2
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "TeamWin"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 10
      numPlayer: 1
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: "NotTeamWin"
      resetValue: 0
    # Легендарная команда
    - tid: 10065
      name: "Легендарная команда"
      book: -2
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "TeamWin"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 10
      numPlayer: 1
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: "NotTeamWin"
      resetValue: 0
    # Знаток прыжка
    - tid: 10066
      name: "Знаток прыжка"
      book: -2
      event: 29
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: client
      property: "PerfectJumpCount"
      function: "MaxPerfectJumpCombo"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 3
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [1, 2, 3, 4]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 0, 0, 0]
      points: 10
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Мастер прыжка
    - tid: 10067
      name: "Мастер прыжка"
      book: -2
      event: 29
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: client
      property: "PerfectJumpCount"
      function: "MaxPerfectJumpCombo"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 3
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [1, 2, 3, 4]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 0, 0, 0]
      points: 10
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Помешанный на прыжках
    - tid: 10068
      name: "Помешанный на прыжках"
      book: -2
      event: 18
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "PerfectJump"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 31
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: "NotPerfectJump"
      resetValue: 0
    # Мастер прыжков
    - tid: 10069
      name: "Мастер прыжков"
      book: -2
      event: 18
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "PerfectJump"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 31
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: "NotPerfectJump"
      resetValue: 0
    # Не унывайте
    - tid: 10070
      name: "Не унывайте"
      book: -2
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "Retire"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 31
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: "GoalIn"
      resetValue: 0
    # Аж слеза наворачивается
    - tid: 10071
      name: "Аж слеза наворачивается"
      book: -2
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "Retire"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 31
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: "GoalIn"
      resetValue: 0
    # Хорошая работа
    - tid: 10072
      name: "Хорошая работа"
      book: -2
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "GoalIn"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 31
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [3, 4, 5, 6]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 0, 0, 0]
      points: 10
      resetEvent: 2
      resetFunction: "Retire"
      resetValue: 0
    # Отлично
    - tid: 10073
      name: "Отлично"
      book: -2
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "GoalIn"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 31
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [5, 6, 7, 8]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 0, 0, 0]
      points: 10
      resetEvent: 2
      resetFunction: "Retire"
      resetValue: 0
    # Великолепно
    - tid: 10074
      name: "Великолепно"
      book: -2
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "GoalIn"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 31
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [7, 8, 9, 10]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 0, 0, 0]
      points: 10
      resetEvent: 2
      resetFunction: "Retire"
      resetValue: 0
    # Поразительно
    - tid: 10075
      name: "Поразительно"
      book: -2
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "GoalIn"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 31
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: "Retire"
      resetValue: 0
    # Просто было интересно..!!
    - tid: 10076
      name: "Просто было интересно..!!"
      book: -2
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "GoalIn"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 31
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: "Retire"
      resetValue: 0
    # Не оторваться
    - tid: 10077
      name: "Не оторваться"
      book: -2
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "GoalIn"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 31
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [10, 11, 12, 13]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 0, 0, 0]
      points: 10
      resetEvent: 4
      resetFunction: "TRUE"
      resetValue: 0
    # Восторг
    - tid: 10078
      name: "Восторг"
      book: -2
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "GoalIn"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 31
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [20, 21, 22, 23]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 0, 0, 0]
      points: 10
      resetEvent: 4
      resetFunction: "TRUE"
      resetValue: 0
    # Полное забвение
    - tid: 10079
      name: "Полное забвение"
      book: -2
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "GoalIn"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 31
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: "RetireOrDisconnect"
      resetValue: 0
    # 1000 идеальных прыжков
    - tid: 10080
      name: "1000 идеальных прыжков"
      book: -2
      event: 18
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "PerfectJump"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 3
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # 2000 идеальных прыжков
    - tid: 10081
      name: "2000 идеальных прыжков"
      book: -2
      event: 18
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "PerfectJump"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 3
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # 3000 идеальных прыжков
    - tid: 10082
      name: "3000 идеальных прыжков"
      book: -2
      event: 18
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "PerfectJump"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 3
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # 4000 идеальных прыжков
    - tid: 10083
      name: "4000 идеальных прыжков"
      book: -2
      event: 18
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "PerfectJump"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 3
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # 5000 идеальных прыжков
    - tid: 10084
      name: "5000 идеальных прыжков"
      book: -2
      event: 18
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "PerfectJump"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 3
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Пора взрослеть
    - tid: 10085
      name: "Пора взрослеть"
      book: -2
      event: 19
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "BreedTry"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Первая любовь
    - tid: 10086
      name: "Первая любовь"
      book: -2
      event: 19
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "BreedSuccess"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Горечь испытания
    - tid: 10087
      name: "Горечь испытания"
      book: -2
      event: 19
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "BreedFail"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Зелёная любовь
    - tid: 10088
      name: "Зелёная любовь"
      book: -2
      event: 0
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "BreedTry"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Это и есть любовь..?
    - tid: 10089
      name: "Это и есть любовь..?"
      book: -2
      event: 0
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "BreedTry"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Жажда любви
    - tid: 10090
      name: "Жажда любви"
      book: -2
      event: 0
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "BreedTry"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Любви всегда мало
    - tid: 10091
      name: "Любви всегда мало"
      book: -2
      event: 0
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "BreedTry"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Горький вкус жизни
    - tid: 10092
      name: "Горький вкус жизни"
      book: -2
      event: 0
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "BreedFail"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Любовь! Или страх?
    - tid: 10093
      name: "Любовь! Или страх?"
      book: -2
      event: 0
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "BreedFail"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Отдать всего себя
    - tid: 10094
      name: "Отдать всего себя"
      book: -2
      event: 0
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "BreedRegister"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: false
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Я звезда
    - tid: 10095
      name: "Я звезда"
      book: -2
      event: 0
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "BreedHorse"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: false
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: "BreedOtherHorse"
      resetValue: 0
    # Я перелётная птица
    - tid: 10096
      name: "Я перелётная птица"
      book: -2
      event: 0
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "BreedHorse"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: false
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: "BreedOtherHorse"
      resetValue: 0
    # Порода что надо
    - tid: 10097
      name: "Порода что надо"
      book: -2
      event: 0
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "BreedSuccess"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: "BreedFail"
      resetValue: 0
    # Одержимость любовью
    - tid: 10098
      name: "Одержимость любовью"
      book: -2
      event: 0
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "BreedSuccess"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: "BreedFail"
      resetValue: 0
    # Мой малыш - вундеркинд
    - tid: 10099
      name: "Мой малыш - вундеркинд"
      book: -2
      event: 0
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "ChildGrade10_15"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: false
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Мой малыш - гений
    - tid: 10100
      name: "Мой малыш - гений"
      book: -2
      event: 0
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "ChildGrade16over"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: false
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Небывалая удача
    - tid: 10101
      name: "Небывалая удача"
      book: -2
      event: 0
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "FestivalReward"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: false
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Миллион в кармане
    - tid: 10102
      name: "Миллион в кармане"
      book: -2
      event: 0
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "FestivalReward"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: false
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Любитель сорвать куш
    - tid: 10103
      name: "Любитель сорвать куш"
      book: -2
      event: 0
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "FestivalReward"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: false
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Помешанный на праздниках
    - tid: 10104
      name: "Помешанный на праздниках"
      book: -2
      event: 0
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "FestivalJoin"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: false
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Заезд с семью друзьями
    - tid: 10105
      name: "Заезд с семью друзьями"
      book: -2
      event: 0
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "InGameFriendsCount"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: false
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 31
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Положение обязывает
    - tid: 10106
      name: "Положение обязывает"
      book: -2
      event: 19
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "SlipStreamMinorSupplyTime"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: false
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 31
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Добродетель уступки
    - tid: 10107
      name: "Добродетель уступки"
      book: -2
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "NoSpurRanking"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: false
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 31
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Ни царапины
    - tid: 10108
      name: "Ни царапины"
      book: -2
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "NoPlayerCollisionRanking"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: false
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 31
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Друг животных
    - tid: 10109
      name: "Друг животных"
      book: -2
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "NoNPCCollision"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: false
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 31
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Тебя-то я подтолкну
    - tid: 10110
      name: "Тебя-то я подтолкну"
      book: -2
      event: 19
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "CollisionPush"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: false
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 31
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Король последних
    - tid: 10111
      name: "Король последних"
      book: -2
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "KingOfLosers"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: false
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 31
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # И всё-таки
    - tid: 10112
      name: "И всё-таки"
      book: -2
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "NoJumpRanking"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: false
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Так и упасть недолго
    - tid: 10113
      name: "Так и упасть недолго"
      book: -2
      event: 33
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: client
      property: "MaxSlidingTime"
      function: "MaxSlidingTime"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 31
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [1, 2, 3, 4]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 0, 0, 0]
      points: 10
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Лидер от старта до финиша
    - tid: 10114
      name: "Лидер от старта до финиша"
      book: -2
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "StartingWinner"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: false
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 31
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Продержаться в планировании дольше 10 секунд
    - tid: 10115
      name: "Продержаться в планировании дольше 10 секунд"
      book: -2
      event: 0
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "GlidingTime"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: false
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 31
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Доехать до финиша без единого столкновения
    - tid: 10116
      name: "Доехать до финиша без единого столкновения"
      book: -2
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "NoCollisionGoalIn"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: false
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 31
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Доехать до финиша без единого прыжка
    - tid: 10117
      name: "Доехать до финиша без единого прыжка"
      book: -2
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "NoJumpGoalIn"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: false
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 31
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Не сдавайтесь
    - tid: 10118
      name: "Не сдавайтесь"
      book: -2
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "GoalInRanking"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 31
      numPlayer: 8
      compareType: 0
      successType: 1
      thresholds: [1, 2, 3, 4]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 0, 0, 0]
      points: 10
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Достичь 10 уровня
    - tid: 10119
      name: "Достичь 10 уровня"
      book: -2
      event: 5
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "Level"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Вырастить лошадь до максимального уровня
    - tid: 10120
      name: "Вырастить лошадь до максимального уровня"
      book: -2
      event: 5
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "HorseLevel"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Вот это богатство!
    - tid: 10121
      name: "Вот это богатство!"
      book: -2
      event: 0
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "Money"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: false
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Шопинг захватил
    - tid: 10122
      name: "Шопинг захватил"
      book: -2
      event: 0
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "BuyMoneyTotal"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: false
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Бывший лихач
    - tid: 10123
      name: "Бывший лихач"
      book: -2
      event: 35
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: client
      property: "HotRoddingReversalCount"
      function: "HotRoddingReversalCount"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 12
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [1, 2, 3, 4]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 0, 0, 0]
      points: 10
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Сбросить соперника магией камеры
    - tid: 10124
      name: "Сбросить соперника магией камеры"
      book: -2
      event: 0
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "TwistCauseReset"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: false
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 12
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Попасть атакующей магией по сопернику в планировании
    - tid: 10125
      name: "Попасть атакующей магией по сопернику в планировании"
      book: -2
      event: 0
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "MagicCauseFalling"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: false
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 12
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Полностью израсходовать силы лошади
    - tid: 10126
      name: "Полностью израсходовать силы лошади"
      book: -2
      event: 0
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "HorseKnockdown"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: false
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Назвать лошадь так же, как другой игрок
    - tid: 10127
      name: "Назвать лошадь так же, как другой игрок"
      book: -2
      event: 0
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "HorseSameName"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: false
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Отомстить тому, кто вас атаковал
    - tid: 10128
      name: "Отомстить тому, кто вас атаковал"
      book: -2
      event: 0
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "MagicRevenge"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: false
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 12
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Поднять навык ухода до 5 уровня
    - tid: 10129
      name: "Поднять навык ухода до 5 уровня"
      book: -2
      event: 0
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "TakecareLevel"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: false
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Я маг
    - tid: 10130
      name: "Я маг"
      book: -2
      event: 42
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "UseAllMagic"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 12
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [1, 2, 3, 4]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 0, 0, 0]
      points: 10
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Фотогеничность 1
    - tid: 10131
      name: "Фотогеничность 1"
      book: -2
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "GoalInWithSpur"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: false
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 31
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Фотогеничность 2
    - tid: 10132
      name: "Фотогеничность 2"
      book: -2
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "GoalInWithSliding"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: false
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 31
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Фотогеничность 3
    - tid: 10133
      name: "Фотогеничность 3"
      book: -2
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "GoalInWithGliding"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: false
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 31
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Покоритель
    - tid: 10134
      name: "Покоритель"
      book: -2
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "GoalInAllMap"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: false
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Мир покорён
    - tid: 10135
      name: "Мир покорён"
      book: -2
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "WinAllMap"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: false
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Безудержный галоп
    - tid: 10136
      name: "Безудержный галоп"
      book: -2
      event: 17
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "MaxVelocity"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: false
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Сверхсветовое буйство
    - tid: 10137
      name: "Сверхсветовое буйство"
      book: -2
      event: 0
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "MaxVelocity"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: false
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Одержимость скоростью
    - tid: 10138
      name: "Одержимость скоростью"
      book: -2
      event: 0
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "MaxVelocity"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: false
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Паровоз без тормозов
    - tid: 10139
      name: "Паровоз без тормозов"
      book: -2
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "ReversalWinner"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 31
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [1, 2, 3, 4]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 0, 0, 0]
      points: 10
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Рывок 4-й ступени
    - tid: 10140
      name: "Рывок 4-й ступени"
      book: -2
      event: 17
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "SpurLevel"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: false
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Рывок 5-й ступени
    - tid: 10141
      name: "Рывок 5-й ступени"
      book: -2
      event: 17
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "SpurLevel"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: false
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [0, 0, 0, 0]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Идеальный прыжок на волоске
    - tid: 10142
      name: "Идеальный прыжок на волоске"
      book: -2
      event: 29
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: client
      property: "PerfectJumpCount"
      function: "PerfectJump"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 3
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [1, 2, 3, 4]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 0, 0, 0]
      points: 10
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Прозрение в прыжке 1
    - tid: 10143
      name: "Прозрение в прыжке 1"
      book: -2
      event: 29
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: client
      property: "PerfectJumpCount"
      function: "PerfectJump"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 3
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [30, 31, 32, 33]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 0, 0, 0]
      points: 10
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Прозрение в прыжке 2
    - tid: 10144
      name: "Прозрение в прыжке 2"
      book: -2
      event: 29
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: client
      property: "PerfectJumpCount"
      function: "PerfectJump"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 3
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [50, 51, 52, 53]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 0, 0, 0]
      points: 10
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Свободны? Может, заезд?
    - tid: 10145
      name: "Свободны? Может, заезд?"
      book: -2
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "GoalIn_14h_16h"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 31
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [1, 2, 3, 4]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 0, 0, 0]
      points: 10
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # После школы - в Алисию!
    - tid: 10146
      name: "После школы - в Алисию!"
      book: -2
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "GoalIn_16h_18h"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 31
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [30, 31, 32, 33]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 0, 0, 0]
      points: 10
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Вечерний человек
    - tid: 10147
      name: "Вечерний человек"
      book: -2
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "GoalIn_19h_22h"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 31
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [30, 31, 32, 33]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 0, 0, 0]
      points: 10
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Первое имя
    - tid: 10148
      name: "Первое имя"
      book: -2
      event: 47
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "TRUE"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [1, 2, 3, 4]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 0, 0, 0]
      points: 10
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Основы усвоены
    - tid: 10149
      name: "Основы усвоены"
      book: -2
      event: 45
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: client
      property: "IntroEnd"
      function: "TRUE"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [1, 2, 3, 4]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 0, 0, 0]
      points: 10
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # В гости
    - tid: 10150
      name: "В гости"
      book: -2
      event: 48
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "TRUE"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [1, 2, 3, 4]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [10, 0, 0, 0]
      points: 0
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Шарики лопнули
    - tid: 10151
      name: "Шарики лопнули"
      book: -2
      event: 46
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: client
      property: "BalloonLapTime"
      function: "TRUE"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [1, 2, 3, 4]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [10, 0, 0, 0]
      points: 0
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # С шариками весело!
    - tid: 10152
      name: "С шариками весело!"
      book: -2
      event: 46
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: client
      property: "BalloonLapTime"
      function: "BalloonLapTime"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [1, 2, 3, 4]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [10, 0, 0, 0]
      points: 0
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Знаток шариков
    - tid: 10153
      name: "Знаток шариков"
      book: -2
      event: 46
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: client
      property: "BalloonLapTime"
      function: "BalloonLapTime"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [1, 2, 3, 4]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [10, 0, 0, 0]
      points: 0
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Свидетельство по уходу за лошадью
    - tid: 10154
      name: "Свидетельство по уходу за лошадью"
      book: -2
      event: 5
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "Level"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [1, 2, 3, 4]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 0, 0, 0]
      points: 10
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Свежий ветер планирования
    - tid: 10155
      name: "Свежий ветер планирования"
      book: 1
      event: 43
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: client
      property: "GlidingDistance"
      function: "GlidingDistance"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 127
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [1, 100, 500, 1800]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Весёлый рывок
    - tid: 10156
      name: "Весёлый рывок"
      book: 1
      event: 17
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "TRUE"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 35
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [1, 100, 2000, 6000]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Прямо как супергерой
    - tid: 10157
      name: "Прямо как супергерой"
      book: 1
      event: 27
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: client
      property: "GlidingSpurCount"
      function: "GlidingSpur"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 35
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [1, 100, 200, 6000]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Волнующая покупка
    - tid: 10158
      name: "Волнующая покупка"
      book: 1
      event: 3
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "BuyItem"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 3
      successType: 0
      thresholds: [500, 10000, 50000, 200000]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Хорошее начало
    - tid: 10159
      name: "Хорошее начало"
      book: 1
      event: 28
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: client
      property: "PerfectStart"
      function: "PerfectStart"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 127
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [1, 150, 300, 1500]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Прозрение в прыжке
    - tid: 10160
      name: "Прозрение в прыжке"
      book: 1
      event: 29
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: client
      property: "PerfectJumpCount"
      function: "PerfectJump"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 35
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [5, 500, 2500, 10000]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Хорошая работа
    - tid: 10161
      name: "Хорошая работа"
      book: 1
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "GoalIn"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 127
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [3, 6, 10, 15]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 2
      resetFunction: "Retire"
      resetValue: 0
    # Не оторваться
    - tid: 10162
      name: "Не оторваться"
      book: 1
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "GoalIn"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 127
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [2, 5, 10, 20]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 4
      resetFunction: "TRUE"
      resetValue: 0
    # Утренний человек
    - tid: 10163
      name: "Утренний человек"
      book: 1
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "GoalIn_8h_13h"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 127
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [2, 20, 100, 500]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # После школы - в Алисию!
    - tid: 10164
      name: "После школы - в Алисию!"
      book: 1
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "GoalIn_16h_18h"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 127
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [2, 20, 100, 500]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Вечерний человек
    - tid: 10165
      name: "Вечерний человек"
      book: 1
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "GoalIn_19h_22h"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 127
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [2, 20, 100, 500]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Умелая лошадь
    - tid: 10166
      name: "Умелая лошадь"
      book: 1
      event: 68
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "TRUE"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 127
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [1, 2, 5, 15]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Чистая грива
    - tid: 10167
      name: "Чистая грива"
      book: 2
      event: 51
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "TRUE"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [3, 70, 500, 5000]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Чистый корпус
    - tid: 10168
      name: "Чистый корпус"
      book: 2
      event: 50
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "TRUE"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [3, 70, 500, 5000]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Чистый хвост
    - tid: 10169
      name: "Чистый хвост"
      book: 2
      event: 52
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "TRUE"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [3, 70, 500, 5000]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Разнообразное питание
    - tid: 10170
      name: "Разнообразное питание"
      book: 2
      event: 53
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "ForciblyFeeding"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [3, 10, 30, 40]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Только любимое
    - tid: 10171
      name: "Только любимое"
      book: 2
      event: 53
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "FavoriteFoodsFeeding"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [3, 50, 500, 10000]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Сытно накормить
    - tid: 10172
      name: "Сытно накормить"
      book: 2
      event: 53
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "Stuffed"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [3, 50, 300, 2000]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # До новой встречи
    - tid: 10173
      name: "До новой встречи"
      book: 2
      event: 55
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "TRUE"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [1, 3, 5, 20]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Это всё из-за меня
    - tid: 10174
      name: "Это всё из-за меня"
      book: 2
      event: 69
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "TRUE"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 31
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [1, 5, 30, 200]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Лёгкая травма? Само пройдёт!
    - tid: 10175
      name: "Лёгкая травма? Само пройдёт!"
      book: 2
      event: 71
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "TRUE"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [1, 3, 5, 50]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Элегантный вид
    - tid: 10176
      name: "Элегантный вид"
      book: 2
      event: 49
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "PolishUp"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [1, 10, 150, 1000]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Почти укротитель
    - tid: 10177
      name: "Почти укротитель"
      book: 2
      event: 54
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "TraningSuccess"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [1, 20, 300, 1000]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Жажда любви
    - tid: 10178
      name: "Жажда любви"
      book: 3
      event: 10
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "TRUE"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [1, 5, 30, 100]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Доктор любовных наук
    - tid: 10179
      name: "Доктор любовных наук"
      book: 3
      event: 11
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "TRUE"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [1, 5, 10, 50]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Горький вкус жизни
    - tid: 10180
      name: "Горький вкус жизни"
      book: 3
      event: 12
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "TRUE"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [1, 10, 30, 50]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Отдать всего себя
    - tid: 10181
      name: "Отдать всего себя"
      book: 3
      event: 13
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "TRUE"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [1, 10, 100, 250]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Потенциал!
    - tid: 10182
      name: "Потенциал!"
      book: 3
      event: 11
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "PotentialCapacities"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [1, 3, 5, 25]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Порода что надо
    - tid: 10183
      name: "Порода что надо"
      book: 3
      event: 11
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "BreedingSuccessCombo"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [2, 2, 2, 2]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [1, 2, 3, 4]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Одержимость любовью
    - tid: 10184
      name: "Одержимость любовью"
      book: 3
      event: 10
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "TRUE"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 0
      thresholds: [2, 3, 4, 5]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 8
      resetFunction: "TRUE"
      resetValue: 0
    # Вся надежда на тебя
    - tid: 10185
      name: "Вся надежда на тебя"
      book: 3
      event: 14
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "MaleHorseCombo"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [3, 3, 3, 3]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [1, 3, 5, 8]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Осталось раскрыть талант
    - tid: 10186
      name: "Осталось раскрыть талант"
      book: 3
      event: 56
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "PotentialCapacitiesGrow"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [1, 3, 5, 25]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Гадкий утёнок
    - tid: 10187
      name: "Гадкий утёнок"
      book: 3
      event: 11
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "GradeCompare"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [1, 3, 10, 50]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Не сдавайтесь
    - tid: 10188
      name: "Не сдавайтесь"
      book: 4
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "GoalInRanking"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [8, 8, 8, 8]
      gameModeFlag: 127
      numPlayer: 8
      compareType: 0
      successType: 1
      thresholds: [5, 30, 150, 600]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Я маг
    - tid: 10189
      name: "Я маг"
      book: 4
      event: 42
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "UseAllMagic"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [3, 3, 3, 3]
      gameModeFlag: 76
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [5, 50, 500, 2000]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Гнать опасно
    - tid: 10190
      name: "Гнать опасно"
      book: 4
      event: 24
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: client
      property: "CourseOut"
      function: "Cliff"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 127
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [1, 3, 5, 30]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Паровоз без тормозов
    - tid: 10191
      name: "Паровоз без тормозов"
      book: 4
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "ReversalWinner"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 127
      numPlayer: 2
      compareType: 0
      successType: 1
      thresholds: [5, 50, 100, 500]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Праздник, от которого сердце замирает
    - tid: 10192
      name: "Праздник, от которого сердце замирает"
      book: 4
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "CarnivalSuccess"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 31
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [5, 30, 150, 400]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Операция удалась
    - tid: 10193
      name: "Операция удалась"
      book: 4
      event: 42
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "JumpParalysis"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [3, 3, 3, 3]
      gameModeFlag: 76
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [5, 30, 150, 400]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Бывший лихач
    - tid: 10194
      name: "Бывший лихач"
      book: 4
      event: 35
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: client
      property: "HotRoddingReversalCount"
      function: "HotRoddingReversalCount"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [3, 3, 3, 3]
      gameModeFlag: 76
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [3, 10, 50, 150]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Фотогеничность 1
    - tid: 10195
      name: "Фотогеничность 1"
      book: 4
      event: 57
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: client
      property: "GoalInUseSpur"
      function: "TRUE"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 35
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [20, 100, 300, 2000]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Фотогеничность 2
    - tid: 10196
      name: "Фотогеничность 2"
      book: 4
      event: 58
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: client
      property: "GoalInUseSliding"
      function: "TRUE"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 127
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [20, 100, 300, 2000]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Фотогеничность 3
    - tid: 10197
      name: "Фотогеничность 3"
      book: 4
      event: 59
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: client
      property: "GoalInUseGliding"
      function: "TRUE"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 127
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [20, 100, 300, 2000]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Начальник следствия
    - tid: 10198
      name: "Начальник следствия"
      book: 5
      event: 23
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: client
      property: "ScreenCapture"
      function: "TRUE"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [5, 20, 50, 500]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Повезло с командой
    - tid: 10199
      name: "Повезло с командой"
      book: 5
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "TeamWinTheLast"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 10
      numPlayer: 8
      compareType: 0
      successType: 1
      thresholds: [5, 30, 100, 200]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Как-то само вышло
    - tid: 10200
      name: "Как-то само вышло"
      book: 5
      event: 72
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: client
      property: "GoalInUseBooster"
      function: "TRUE"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 76
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [1, 100, 250, 500]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Моя лошадь - финишёр
    - tid: 10201
      name: "Моя лошадь - финишёр"
      book: 5
      event: 57
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: client
      property: "GoalInUseSpur"
      function: "Spur3thGoalIn"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 35
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [1, 50, 150, 300]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Красота важнее всего
    - tid: 10202
      name: "Красота важнее всего"
      book: 5
      event: 60
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: client
      property: "GetSpurOnGliding"
      function: "TRUE"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 35
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [1, 300, 700, 1500]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Не болей
    - tid: 10203
      name: "Не болей"
      book: 5
      event: 70
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "TRUE"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [1, 20, 50, 200]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Опыт с крохой
    - tid: 10204
      name: "Опыт с крохой"
      book: 5
      event: 11
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "PoorHorse"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [400, 400, 400, 400]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [1, 2, 3, 5]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Опыт с богатырём
    - tid: 10205
      name: "Опыт с богатырём"
      book: 5
      event: 11
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "GoodHorse"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [500, 500, 500, 500]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [1, 2, 3, 5]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Держите, это вам
    - tid: 10207
      name: "Держите, это вам"
      book: 6
      event: 62
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "TRUE"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 76
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [5, 30, 100, 800]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Просто так не бью!
    - tid: 10208
      name: "Просто так не бью!"
      book: 6
      event: 42
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "CriticalFireballAttack"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 76
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [5, 30, 200, 1000]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Просто так не отпущу!
    - tid: 10209
      name: "Просто так не отпущу!"
      book: 6
      event: 42
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "RevengeAttackWithFireball"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 12
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [5, 10, 25, 70]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Целюсь только в тебя
    - tid: 10210
      name: "Целюсь только в тебя"
      book: 6
      event: 42
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "ComboAttackSuccessWithFireball"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [3, 3, 3, 3]
      gameModeFlag: 76
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [5, 10, 25, 50]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Момент настал
    - tid: 10211
      name: "Момент настал"
      book: 6
      event: 42
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "ShieldEndAttackWithFireball"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [3, 3, 3, 3]
      gameModeFlag: 76
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [5, 10, 25, 100]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Вершина удовольствия
    - tid: 10212
      name: "Вершина удовольствия"
      book: 6
      event: 74
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: client
      property: "MidairAttackFireballCount"
      function: "TRUE"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 76
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [5, 10, 25, 50]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Кровная месть
    - tid: 10213
      name: "Кровная месть"
      book: 6
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "Revenge"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 15
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [3, 10, 25, 50]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Так не пойдёт
    - tid: 10214
      name: "Так не пойдёт"
      book: 6
      event: 42
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "FireBallSuccess"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 76
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [3, 15, 70, 400]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # С оруженосцами справлюсь 1
    - tid: 10216
      name: "С оруженосцами справлюсь 1"
      book: 7
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "WinAI"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 32
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [1, 5, 10, 20]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # С оруженосцами справлюсь 2
    - tid: 10217
      name: "С оруженосцами справлюсь 2"
      book: 7
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "WinAI"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 64
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [1, 5, 10, 20]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Небывалая удача
    - tid: 10218
      name: "Небывалая удача"
      book: 7
      event: 11
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "GoodChild"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [1, 2, 3, 4]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Подсыпать перца
    - tid: 10219
      name: "Подсыпать перца"
      book: 7
      event: 42
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "LastSpurtAttack"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 76
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [5, 30, 50, 300]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # В любой ситуации
    - tid: 10220
      name: "В любой ситуации"
      book: 7
      event: 61
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: client
      property: "PerfectSpurCombo"
      function: "PerfectSpurMaster"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 35
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [3, 50, 500, 2000]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Мастер тайминга
    - tid: 10221
      name: "Мастер тайминга"
      book: 7
      event: 54
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "TrainingCombo"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [3, 3, 3, 3]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [3, 50, 200, 400]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Безупречно
    - tid: 10222
      name: "Безупречно"
      book: 7
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "TeamPerfectWinNew"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 10
      numPlayer: 4
      compareType: 0
      successType: 1
      thresholds: [3, 20, 50, 150]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Мастер рывка
    - tid: 10223
      name: "Мастер рывка"
      book: 7
      event: 61
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: client
      property: "PerfectSpurCombo"
      function: "PerfectSpurCombo"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [3, 3, 3, 3]
      gameModeFlag: 35
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [3, 100, 300, 1000]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Удачный день
    - tid: 10224
      name: "Удачный день"
      book: 7
      event: 54
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "TrainingCritical"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [3, 70, 300, 1000]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Во всей красе
    - tid: 10225
      name: "Во всей красе"
      book: 8
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "Win"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 1
      numPlayer: 4
      compareType: 0
      successType: 1
      thresholds: [1, 10, 50, 100]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Нет равных
    - tid: 10226
      name: "Нет равных"
      book: 8
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "Win"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 4
      numPlayer: 4
      compareType: 0
      successType: 1
      thresholds: [1, 10, 50, 100]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Мы - одно целое
    - tid: 10227
      name: "Мы - одно целое"
      book: 8
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "TeamWin"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 2
      numPlayer: 4
      compareType: 0
      successType: 1
      thresholds: [1, 10, 50, 300]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Пусть приходит кто хочет
    - tid: 10228
      name: "Пусть приходит кто хочет"
      book: 8
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "TeamWin"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 8
      numPlayer: 4
      compareType: 0
      successType: 1
      thresholds: [1, 10, 50, 300]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Знаток прыжка
    - tid: 10229
      name: "Знаток прыжка"
      book: 8
      event: 29
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: client
      property: "PerfectJumpCount"
      function: "MaxPerfectJumpCombo"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [10, 10, 10, 10]
      gameModeFlag: 35
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [30, 200, 500, 2000]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Так и упасть недолго
    - tid: 10230
      name: "Так и упасть недолго"
      book: 8
      event: 33
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: client
      property: "MaxSlidingTime"
      function: "SlidingTime"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [2, 2, 2, 2]
      gameModeFlag: 127
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [20, 300, 700, 3000]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # За пределом
    - tid: 10231
      name: "За пределом"
      book: 8
      event: 17
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "MaxSpurCount"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [3, 3, 3, 3]
      gameModeFlag: 35
      numPlayer: 0
      compareType: 2
      successType: 0
      thresholds: [4, 5, 7, 10]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Безудержный галоп
    - tid: 10232
      name: "Безудержный галоп"
      book: 8
      event: 38
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: client
      property: "MaxVelocity"
      function: "GetMaxSpeed"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 127
      numPlayer: 0
      compareType: 2
      successType: 0
      thresholds: [110, 120, 123, 125]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Помешанный на шариках
    - tid: 10233
      name: "Помешанный на шариках"
      book: 8
      event: 46
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: client
      property: "BalloonLapTime"
      function: "BalloonLapTime"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 1
      successType: 0
      thresholds: [31, 30, 29, 28]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Хватит уже, нет?
    - tid: 10234
      name: "Хватит уже, нет?"
      book: 8
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "MaxWinStop"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 5
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [3, 15, 30, 70]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Перфекционист
    - tid: 10235
      name: "Перфекционист"
      book: 8
      event: 61
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: client
      property: "PerfectSpurCombo"
      function: "TRUE"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 35
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [1, 100, 1000, 5000]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Сильнейшая команда!
    - tid: 10236
      name: "Сильнейшая команда!"
      book: 8
      event: 2
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "MaxWinStop"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 10
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [3, 15, 30, 70]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [400, 800, 1200, 2000]
      points: 1
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Первый вход в магазин
    - tid: 20001
      name: "Первый вход в магазин"
      book: 0
      event: 65
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "True"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [1, 1, 1, 1]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 0
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Первый вход в снаряжение
    - tid: 20002
      name: "Первый вход в снаряжение"
      book: 0
      event: 66
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "True"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [1, 1, 1, 1]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 0
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Первый вход в уход за лошадью
    - tid: 20003
      name: "Первый вход в уход за лошадью"
      book: 0
      event: 67
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "True"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [1, 1, 1, 1]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 0
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Первое имя лошади
    - tid: 20004
      name: "Первое имя лошади"
      book: 0
      event: 47
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "True"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [1, 1, 1, 1]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 0
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Вступление пройдено
    - tid: 20005
      name: "Вступление пройдено"
      book: 0
      event: 45
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: client
      property: "IntroEnd"
      function: "True"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [1, 1, 1, 1]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 0
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Служебная запись
    - tid: 20000
      name: "Служебная запись"
      book: 0
      event: 45
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: client
      property: "IntroEnd"
      function: "False"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [0, 0, 0, 0]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [1, 1, 1, 1]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 0
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
      # ★Заведомо невыполнимая заглушка — НИКОГДА не выдавать:
      # выдача закроет системную книгу навсегда.
      neverAward: true
    # Уровень 2
    - tid: 20006
      name: "Уровень 2"
      book: 0
      event: 5
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "LevelCheck"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [2, 2, 2, 2]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [1, 1, 1, 1]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 0
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Уровень 3
    - tid: 20007
      name: "Уровень 3"
      book: 0
      event: 5
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: server
      function: "LevelCheck"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [3, 3, 3, 3]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [1, 1, 1, 1]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 0
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Разговор с NPC на 2 уровне
    - tid: 20008
      name: "Разговор с NPC на 2 уровне"
      book: 0
      event: 75
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: client
      property: "NPCDialogLevel"
      function: "DialogLevelCheck"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [2, 2, 2, 2]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [1, 1, 1, 1]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 0
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Разговор с NPC на 5 уровне
    - tid: 20009
      name: "Разговор с NPC на 5 уровне"
      book: 0
      event: 75
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: client
      property: "NPCDialogLevel"
      function: "DialogLevelCheck"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [5, 5, 5, 5]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [1, 1, 1, 1]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 0
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Разговор с NPC на 12 уровне
    - tid: 20010
      name: "Разговор с NPC на 12 уровне"
      book: 0
      event: 75
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: client
      property: "NPCDialogLevel"
      function: "DialogLevelCheck"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [12, 12, 12, 12]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [1, 1, 1, 1]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 0
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Разговор с NPC на 8 уровне
    - tid: 20011
      name: "Разговор с NPC на 8 уровне"
      book: 0
      event: 75
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: client
      property: "NPCDialogLevel"
      function: "DialogLevelCheck"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [8, 8, 8, 8]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [1, 1, 1, 1]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 0
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
    # Разговор с NPC на 10 уровне
    - tid: 20012
      name: "Разговор с NPC на 10 уровне"
      book: 0
      event: 75
      # server = событие приходит серверу его собственными командами;
      # client = приходит только репортом 0x16b, которому нельзя верить на слово.
      source: client
      property: "NPCDialogLevel"
      function: "DialogLevelCheck"
      # Реализован ли предикат в оригинальном ach_conditions.lua: где да —
      # семантика известна точно, где нет — её ещё предстоит определить.
      hasOriginalCondition: true
      functionValues: [10, 10, 10, 10]
      gameModeFlag: 0
      numPlayer: 0
      compareType: 0
      successType: 1
      thresholds: [1, 1, 1, 1]
      # ★Выплаты обнулены решением владельца; оригинал сохранён рядом, чтобы
      # включение экономики было перегенерацией, а не переписыванием.
      rewards: [0, 0, 0, 0]
      originalRewards: [0, 0, 0, 0]
      points: 0
      resetEvent: 0
      resetFunction: ""
      resetValue: 0
